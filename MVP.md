# 🚀 BLDR.EMPIRE v3.0 - MVP (Minimum Viable Product)

## 📋 Что включено в MVP

### Backend (95% готово)
- ✅ Аутентификация и авторизация (JWT + RBAC)
- ✅ СОД (Среда общих данных) - управление документами
- ✅ Версионирование документов
- ✅ Права доступа к документам
- ✅ Классификация документов по ГОСТ 21.101-2020
- ✅ Полнотекстовый и семантический поиск
- ✅ RAG система (базовая)
- ✅ LLM интеграция (базовая)
- ✅ Система памяти для агентов
- ✅ ProcessFactory для генерации процессов

### Frontend (30% готово)
- ✅ Базовая структура (React + TypeScript + Ant Design)
- ✅ Аутентификация (логин/регистрация)
- ✅ Главная страница
- ⏳ СОД UI (в разработке)
- ⏳ Управление процессами (в разработке)

### Инфраструктура
- ✅ Docker Compose для всех сервисов
- ✅ PostgreSQL для основных данных
- ✅ Redis для кэширования
- ✅ MinIO для хранения файлов
- ⏳ Neo4j (опционально для MVP)
- ⏳ Qdrant (опционально для MVP)

## 🚀 Быстрый старт

### Требования
- Docker Desktop (Windows/Mac) или Docker + Docker Compose (Linux)
- Git

### Установка и запуск

#### Windows (PowerShell)
```powershell
# Клонирование репозитория (если еще не клонирован)
git clone https://github.com/s9034541160-source/Bldr.git
cd Bldr

# Инициализация MVP
.\scripts\init_mvp.ps1
```

#### Linux/Mac
```bash
# Клонирование репозитория (если еще не клонирован)
git clone https://github.com/s9034541160-source/Bldr.git
cd Bldr

# Инициализация MVP
chmod +x scripts/init_mvp.sh
./scripts/init_mvp.sh
```

### Ручной запуск

1. **Запуск всех сервисов:**
   ```bash
   docker-compose -f docker-compose.mvp.yml up -d
   ```

2. **Инициализация базы данных:**
   ```bash
   docker-compose -f docker-compose.mvp.yml exec backend python -m alembic upgrade head
   docker-compose -f docker-compose.mvp.yml exec backend python backend/scripts/init_roles.py
   ```

3. **Проверка работы:**
   ```bash
   # Health check
   curl http://localhost:8000/api/health
   
   # API документация
   open http://localhost:8000/api/docs
   ```

## 📍 Доступные сервисы

После запуска MVP доступны:

- **Backend API**: http://localhost:8000
- **API Документация (Swagger)**: http://localhost:8000/api/docs
- **Frontend**: http://localhost:3000
- **MinIO Console**: http://localhost:9001
  - Логин: `minioadmin`
  - Пароль: `minioadmin123`

## 🧪 Тестирование MVP

### Базовые тесты
```bash
# Linux/Mac
chmod +x scripts/test_mvp.sh
./scripts/test_mvp.sh

# Windows
# Используйте Postman или curl вручную
```

### Ручное тестирование через API

1. **Регистрация пользователя:**
   ```bash
   curl -X POST http://localhost:8000/api/auth/register \
     -H "Content-Type: application/json" \
     -d '{"username":"admin","email":"admin@example.com","password":"admin123"}'
   ```

2. **Логин:**
   ```bash
   curl -X POST http://localhost:8000/api/auth/login \
     -H "Content-Type: application/x-www-form-urlencoded" \
     -d "username=admin&password=admin123"
   ```

3. **Использование токена:**
   ```bash
   # Замените YOUR_TOKEN на токен из ответа логина
   curl -X GET http://localhost:8000/api/auth/me \
     -H "Authorization: Bearer YOUR_TOKEN"
   ```

## 📊 Статус MVP

### ✅ Готово к использованию
- Аутентификация и авторизация
- Управление документами (CRUD)
- Версионирование документов
- Права доступа
- Поиск документов
- API документация

### ⏳ В разработке
- Frontend для СОД
- Полная интеграция LLM (требует настройки моделей)
- Neo4j и Qdrant (опционально)
- Бизнес-процессы (109 процессов)

### 🔧 Требует настройки
- LLM модели (нужно скачать и настроить GGUF модели)
- Neo4j (для графовой БД - опционально)
- Qdrant (для векторного поиска - опционально)

## 🎯 Что можно проверить в MVP

1. **Аутентификация:**
   - Регистрация пользователя
   - Логин/логаут
   - JWT токены

2. **Управление документами:**
   - Загрузка документа
   - Просмотр списка документов
   - Поиск документов
   - Версионирование
   - Права доступа

3. **API:**
   - Все эндпоинты доступны через Swagger UI
   - Health checks работают

## 🐛 Известные ограничения MVP

1. **LLM модели:** Требуют отдельной настройки и загрузки GGUF моделей
2. **Neo4j/Qdrant:** Не включены в MVP, но можно добавить при необходимости
3. **Frontend:** Базовая версия, полный UI в разработке
4. **Бизнес-процессы:** Только ProcessFactory готов, сами процессы нужно создавать

## 📝 Следующие шаги после MVP

1. Настроить LLM модели для полной функциональности ИИ
2. Разработать полный Frontend для СОД
3. Реализовать первые бизнес-процессы
4. Добавить мониторинг и логирование
5. Настроить CI/CD

## 🆘 Проблемы и решения

### Проблема: Порты заняты
**Решение:** Измените порты в `docker-compose.mvp.yml`

### Проблема: База данных не инициализируется
**Решение:** 
```bash
docker-compose -f docker-compose.mvp.yml down -v
docker-compose -f docker-compose.mvp.yml up -d
# Подождите 10 секунд и повторите инициализацию
```

### Проблема: Frontend не подключается к Backend
**Решение:** Проверьте `VITE_API_BASE_URL` в `docker-compose.mvp.yml`

---

**Дата создания MVP:** 2025-01-09  
**Версия:** 3.0.0-MVP

