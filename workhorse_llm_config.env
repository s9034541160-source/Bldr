# 🚀 Конфигурация рабочего ансамбля LLM для RTX 4060 (БЕЗ 30B модели)
# Qwen3-8B (рабочая лошадка) + RuT5 (специалист по извлечению)
# Максимальная скорость и эффективность для RAG-тренера

# ===========================================
# РАБОЧАЯ ЛОШАДКА: Qwen3-8B
# ===========================================
# Новейшая модель с thinking mode и 32K контекстом
LLM_WORKHORSE_MODEL=Qwen/Qwen3-8B
LLM_WORKHORSE_MAX_LENGTH=32768

# ===========================================
# СПЕЦИАЛИСТ: RuT5
# ===========================================
# Специализированная модель для извлечения метаданных
LLM_EXTRACTION_MODEL=ai-forever/ruT5-base
LLM_EXTRACTION_MAX_LENGTH=2048

# ===========================================
# НАСТРОЙКИ VRAM ДЛЯ RTX 4060 (8GB)
# ===========================================
# Оптимизация памяти для RTX 4060 (БЕЗ 30B модели!)
LLM_4BIT_QUANTIZATION=true
LLM_8BIT_QUANTIZATION=false
LLM_MAX_MEMORY_GB=7.0

# ===========================================
# ОБЩИЕ НАСТРОЙКИ
# ===========================================
LLM_DEVICE=auto
LLM_CACHE_DIR=I:/models_cache
LLM_TEMPERATURE=0.1

# ===========================================
# НАСТРОЙКИ КЭША HUGGING FACE
# ===========================================
# Перенаправляем все модели на диск I:\
HF_HOME=I:/huggingface_cache
TRANSFORMERS_CACHE=I:/huggingface_cache
HF_DATASETS_CACHE=I:/huggingface_cache

# ===========================================
# СТРАТЕГИЯ ИСПОЛЬЗОВАНИЯ (БЕЗ 30B МОДЕЛИ!)
# ===========================================
# Stage 4 (Классификация): Qwen3-8B - быстрая классификация
# Stage 5.5 (Анализ): Qwen3-8B - быстрый анализ
# Stage 8 (Метаданные): RuT5 - специализированное извлечение
# Stage 10 (Сложная логика): Qwen3-8B - Cypher запросы
# 
# ❌ ИСКЛЮЧЕНО: Qwen3-Coder-30B (слишком тяжелый для RAG-тренера)
# 💡 Результат: Максимальная скорость + качество для RTX 4060
