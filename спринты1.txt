Ниже — **конкретный план работы в Cursor** по спринтам.  
Каждый пункт = **отдельный `.cursor-task.md` файл** (можно создавать прямо в проекте), который ты открываешь в Cursor и выполняешь по шаблону.

---

## 🎯 Шаблон `.cursor-task.md` (сохрани как сниппет)

```markdown
# Task-ID: S1-T1
# Title: <кратко>
# Sprint: S1
# Status: TODO → IN-PROGRESS → DONE

## Цель
<что должно быть готово>

## Контекст
<где находится код, на что влияет>

## Требования
- [ ] требование 1
- [ ] требование 2

## Промпт для Cursor
<codegen>
Язык: TypeScript / Python / Docker
Фреймворк: React / FastAPI / ...
Ограничения: type-safe, eslint-pass, покрыть 1 простым тестом
Добавь: docstring / JSDoc / OpenAPI-annotations
</codegen>

## Пример вызова (curl / UI)
```

---

## 📅 Спринт 0 (1 неделя) – «Инфраструктура»

| Task-ID | Название | Промпт (вставлять в `<codegen>`) |
|---------|----------|------------------------------------|
| **S0-T1** | `docker-compose.yml` для keycloak + postgres + minio + redis + qdrant + nginx | Сгенерируй docker-compose.yml v3.9: keycloak (postgres), minio, qdrant, redis, nginx (reverse-proxy). Порт keycloak 8080, minio 9000+9001, qdrant 6333, redis 6379. Секреты в .env. Health-check для каждого сервиса. |
| **S0-T2** | Скрипт `init-lets-encrypt.sh` | Bash-скрипт: создаёт self-signed SSL (local), папки nginx/ssl, выводит команды для доверия сертификата. |
| **S0-T3** | `.env.example` | Сгенерируй .env.example для S0-T1 (без реальных паролей). |
| **S0-T4** | `Makefile` | Цели: up, down, logs, clean, ps. Использовать docker-compose. |
| **S0-T5** | Папка `infra/` и README.md | Markdown-таблица: сервис, порт, credentials, health-check endpoint. |

**Готовность: 100 % → переходим к S1.**

---

## 📅 Спринт 1 (3 недели) – «Portal»

| Task-ID | Название | Промпт |
|---------|----------|--------|
| **S1-T1** | React-Vite-TS скелет | Создай vite-react-ts проект в `apps/portal/`. Добавь eslint-prettier, path-alias `@/*`, Ant-Design, React-Router v6, Zustand, axios. |
| **S1-T2** | Layout: SideMenu + Header + Content | Ant Design Layout: левое меню (collapsed), верхний тулбар (поиск, уведомления, аватар), контент-область. Меню пункты: Тендеры, ТЭО, ГПР, Бюджет, Документы, Обучение моделей. |
| **S1-T3** | AuthProvider + Keycloak-js | Создай KeycloakProvider (React), автологин, refresh-token, private-route. Сохрани access-token в axios-interceptor. |
| **S1-T4** | React-Router структура | routes/index.tsx: lazy-loading для /tender, /teo, /gantt, /budget, /docs, /train. |
| **S1-T5** | Zustand store – user & notifications | Стор: user (name, roles), notifications (array {id, text, read}). Добавь хук useNotify(). |
| **S1-T6** | Axios-instance + error-interceptor | Инстанс: baseURL=/api, bearer-token, 401 → logout, 502 → toast-error. |
| **S1-T7** | UI-компонент UploadPDF (drag-drop) | React + AntD Upload: accept=".pdf", multiple, превью списка, вызов `POST /api/upload`. |
| **S1-T8** | S3-presigned endpoint | FastAPI `/api/upload` : возвращает presigned-url minio, bucket=uploads, key=uuid.pdf. |
| **S1-T9** | Health-check page | Страница /health: показывает статус каждого сервиса (redis, qdrant, keycloak) через `/api/health`. |
| **S1-T10** | CI GitHub-Actions | workflow: lint-ts, lint-py, docker-build-cache, push to GHCR. |

**Definition of Done S1:**  
`make up` → открываешь `https://localhost` → редирект на Keycloak → вход → видишь SideMenu и можешь загрузить PDF.

---

## 📅 Спринт 2 (3 недели) – «Tender-AI»

| Task-ID | Название | Промпт |
|---------|----------|--------|
| **S2-T1** | FastAPI микросервис `apps/tender-ai/` | Создай отдельный Dockerfile, uv-project, router `/tender/*`, подключается к общей БД (postgres url из .env). |
| **S2-T2** | OCR-container (DeepSeek-OCR) | Dockerfile: base=nvidia/cuda:12.1, установи unsloth, deepseek-ocr. Скрипт `ocr.py input.pdf output.json` (pages: [{page, text, tables}]). |
| **S2-T3** | Endpoint `POST /tender/ocr` | Принимает uuid файла из minio → запускает контейнер OCR → сохраняет `ocr_result.json` в minio → возвращает task_id. |
| **S2-T4** | WebSocket-прогресс OCR | Используй `redis pub/sub`: публикует % каждые 5 сек. Frontend подписывается на `ws:/tender/ws/ocr/{task_id}`. |
| **S2-T5** | LLM-prompt «Извлечь тендерные требования» | Создай промпт-шаблон `tender_extract.jinja2`: вход = ocr-json, выход = JSON-схема {объем-работ, сроки, ограничения, штрафы}. Используй локальный `llama3-8b-q4`. |
| **S2-T6** | Endpoint `POST /tender/extract` | body={uuid}, вызывает LLM, возвращает structured-output, сохраняет в БД. |
| **S2-T7** | UI-страница «Анализ тендера» | React: форма загрузки → прогресс-бар → таблица извлечённых требований → кнопка «Сформировать КП». |
| **S2-T8** | Генерация КП (Word) | Шаблон `templates/kp.docx` + Jinja2 → заполнение → `POST /tender/kp` → возвращает presigned-url docx. |
| **S2-T9** | ЭДО-отправка КП | Интеграция Диадок API: `POST /tender/send-kp` → загружает docx → создает envelope → возвращает ссылку на подпись. |
| **S2-T10** | Unit-тесты (pytest) | Покрой 80 % маршрутов OCR и extract (mock LLM). |

**DoD S2:** загружаешь тендер-PDF → через 30 сек видишь таблицу требований → жмёшь «Сформировать КП» → скачиваешь готовый Word.

---

## 📅 Спринт 3 (3 недели) – «ТЭО-AI»

| Task-ID | Название | Промпт |
|---------|----------|--------|
| **S3-T1** | Микросервис `apps/teo-ai/` | Аналогично S2-T1, router `/teo/*`, подключение к БД. |
| **S3-T2** | Справочник ГЭСН в Qdrant | Скрипт `gesn_ingest.py`: парсит Excel-ГЭСН → чанки → embeddings (sbert) → Qdrant. |
| **S3-T3** | LLM-prompt «Авто-смета по объёму» | Шаблон `smeta_prompt.jinja2`: вход = объем-работ, выход = список {код-ГЭСН, название, ед-изм, кол-во, цена, итого}. |
| **S3-T4** | Endpoint `POST /teo/smeta` | body={tender_uuid}, вызывает RAG-поиск ГЭСН → LLM → возвращает Excel-ссылку. |
| **S3-T5** | UI-страница «Смета» | Таблица AntD Table → редактирование ячеек → кнопка «Пересчитать» (локально). |
| **S3-T6** | Финансовые метрики (NPV, IRR) | Endpoint `POST /teo/fin-metrics`: body={cash_flow[]} → numpy-financial → JSON. |
| **S3-T7** | PDF-ТЭО (WeasyPrint) | Шаблон `teo.html.jinja2` → кнопка «Скачать PDF-ТЭО». |
| **S3-T8** | Согласование ТЭО | BPMN-объект: «ТЭО отправлено» → Telegram-опросы → фиксация статуса в БД. |
| **S3-T9** | Экспорт сметы в 1С | Кнопка «Экспорт в 1С» → формат XML (утилита `1c_xml.py`). |
| **S3-T10** | История изменений сметы | Таблица `smeta_history`, UI-diff (AntD). |

**DoD S3:** выбрал объёмы → получил смету → отредактировал → скачал PDF-ТЭО → отправил на согласование.

---

## 📅 Спринт 4 (3 недели) – «Budget-AI»

| Task-ID | Название | Промпт |
|---------|----------|--------|
| **S4-T1** | Микросервис `apps/budget-ai/` | router `/budget/*`, подключение к БД. |
| **S4-T2** | Модель «Прямые затраты» | Импорт сметы S3 → группировка по ГЭСН-разделам → таблица direct_cost. |
| **S4-T3** | Накладные (НР) | Шаблон `nr_calc.py`: % от прямых затрат, editable. |
| **S4-T4** | Сметная прибыль | Ползунок UI → автоматический расчёт. |
| **S4-T5** | Денежный поток (cash-flow) | Генерация графика (cash-flow) по графику ГПР → Excel-экспорт. |
| **S4-T6** | Monte-Carlo риски | LLM-генерация 3 сценариев (оптим, пессим, реал) → 1000 симуляций → график. |
| **S4-T7** | UI-дашборд | React-Chartjs: затраты, ден-поток, риски. |
| **S4-T8** | PDF-отчёт бюджета | WeasyPrint → 10-страничный отчёт. |
| **S4-T9** | Утверждение бюджета | Telegram-опрос → статус «Утверждён». |
| **S4-T10** | Интеграция с 1С-бюджет | XML-export → ручной импорт в 1С (первый релиз). |

---

## 📅 Спринт 5 (3 недели) – «ГПР-AI»

| Task-ID | Название | Промпт |
|---------|----------|--------|
| **S5-T1** | Микросервис `apps/gantt-ai/` | router `/gantt/*`. |
| **S5-T2** | Импорт РД (Excel) | Парсер `rd_parser.py` → список работ {id, name, vol, unit}. |
| **S5-T3** | RAG-нормы трудоёмкости | Поиск в ГЭСН-векторах → расчёт длительности. |
| **S5-T4** | Критический путь (networkx) | Алгоритм CPM → вывод критических работ. |
| **S5-T5** | UI-Gantt (React) | Библиотека `frappe-gantt-react` → редактирование связей. |
| **S5-T6** | Экспорт в MS Project | `python-mpx` → `.xml` → скачивание. |
| **S5-T7** | Telegram-бот «Наряд» | Ежедневно 9:00 → список крит-работ → отметка выполнения. |
| **S5-T8** | Обновление прогресса | Бот присылает % → обновление Gantt → пересчёт крит-пути. |
| **S5-T9** | Алёрты просрочек | Если дата сдвигается → alert в Telegram + email. |
| **S5-T10** | PDF-график | WeasyPrint → `gantt.pdf`. |

---

## 📅 Спринт 6 (3 недели) – «Train-AI (Unsloth-UI)»

| Task-ID | Название | Промпт |
|---------|----------|--------|
| **S6-T1** | Dockerfile `unsloth-gpu` | CUDA 12.1, python 3.10, unsloth, deepseek-ocr. |
| **S6-T2** | FastAPI-service `apps/train-ai/` | router `/train/*`, GPU-health-check. |
| **S6-T3** | OCR-endpoint `POST /train/ocr` | вход = uuid.pdf → docker-run unsloth-ocr → выход = json в minio. |
| **S6-T4** | UI-страница «Обучение» | 4 шага: Upload → OCR → Q-A → Train (ProgressBar). |
| **S6-T5** | Q-A генератор | Prompt-template `ru_qa.jinja2` → LLM (local) → 10 пар → ручная правка (Monaco-editor). |
| **S6-T6** | Dataset-converter | JSON → HuggingFace Dataset → сохранение в `datasets/{uuid}/`. |
| **S6-T7** | Unsloth-training endpoint | body={dataset_uuid, base_model, epochs, lr, lora_rank} → Celery-task → GPU → LoRA. |
| **S6-T8** | Live-логи WebSocket | Парсинг stdout `unsloth` → % → WS → frontend Chart.js. |
| **S6-T9** | LoRA → GGUF converter | shell `llama.cpp/convert-lora` → итоговый `.gguf` → bucket `models/trained/`. |
| **S6-T10** | Кнопка «Activate model» | symlink в `models/active/new-model.gguf` → перезапуск LM-Studio (API `/reload`). |

**DoD S6:** загрузил PDF → получил Q-A → нажал «Train» → через 2 ч новая модель активна и доступна в списке моделей.

---

## 🧠 Как пользоваться промтами в Cursor

1. Создай файл `.cursor-task.md` в корне проекта (шаблон выше).  
2. Вставь блок `<codegen>...</codegen>` **внутри файла**.  
3. Нажми `Ctrl+K` → выбери «Generate» → Cursor напишет код **в контексте проекта**.  
4. После генерации проставь чек-бокс в `.cursor-task.md` и коммить `git commit -m "S1-T1: done"`.  
5. Переходи к следующему `.cursor-task.md`.

---

**Готов к старту:**  
1. `mkdir bldr-v3 && cd bldr-v3`  
2. Создай `S0-T1.cursor-task.md` → запусти Cursor → нажми Generate.  
3. Идём по списку — и через 18 недель у тебя production-ready BLDR v3.