# 🏗️ BLDR v3: Архитектура и стратегия развития  
*(верхнеуровневое описание и дорожная карта)*

---

## 1. Видение продукта

**BLDR v3** — это **веб-среда для полной цифровой трансформации строительной компании**, в которой:

- **все бизнес-процессы** (от тендера до закрытия проекта) автоматизируются через **дообученные локальные LLM**;
- **все документы** живут в **структурированной СОД** (Single Source of Truth) с версионностью и правами;
- **все взаимодействия** между сотрудниками и внешними контрагентами происходят в **едином цифровом контуре**:  
  веб-интерфейс + Telegram-бот + ЭДО-обмен (Диадок/СБИС/КриптоПро).

**Миссия:** убрать Excel-файлы, электронную почту и бумагу из строительного проекта.

---

## 2. Главные принципы архитектуры

| Принцип | Реализация |
|---------|------------|
| **Model-First** | Любой бизнес-шаг = вызов дообученной **локальной** GGUF-модели (Unsloth → LM-Studio). |
| **Document-as-Code** | Каждый документ — это JSON-контракт + шаблон (Jinja2 / DOCX / PDF). |
| **Single Source of Truth** | Все версии всех файлов хранятся в **S3-compatible** + PostgreSQL (мета) + Qdrant (вектор) + Neo4j (граф связей). |
| **Event-Driven** | Внутренний **event-bus** (Redis Streams) связывает модули: «смета утверждена» → «создать заказ» → «отправить КС-2». |
| **Security-by-Design** | JWT + row-level security + audit-лог + подпись (КриптоПро). |
| **Plugin-Ready** | Каждый отдел = **отдельный микро-фронт** на React + own FastAPI-микросервис. |

---

## 3. Технологический стек

| Слой | Стек |
|------|------|
| **Frontend** | React 18 + TypeScript + Vite + Ant Design + Zustand |
| **Backend** | FastAPI + Python 3.11 + Pydantic v2 + SQLModel |
| **LLM-runtime** | LM-Studio (localhost:1234) + GGUF-Q4_K_M |
| **Doctools** | DeepSeek-OCR (Unsloth) + PyMuPDF + python-docx + Jinja2 |
| **Базы** | PostgreSQL (meta) + Qdrant (768-вектор) + Neo4j (граф) + Redis (cache/queue) |
| **Файловое хранилище** | MinIO (S3-API) /bucket/project_id/v{ver}/... |
| **ЭДО** | Диадок API / СБИС API / КриптоПро CSP |
| **CI/CD** | GitHub Actions + Docker Compose + Watchtower |
| **IDE** | Cursor (AI-режим) |

---

## 4. Модульная структура (high-level)

```
bldr-v3/
├─ apps/                        # микросервисы
│  ├─ portal/                   # основной веб-шлюз (SSR, auth, menu)
│  ├─ tender-ai/                # анализ тендеров (LLM + RAG)
│  ├─ teo-ai/                   # ТЭО, сметы, ГПР
│  ├─ budget-ai/                # бюджет проекта
│  ├─ edo-ai/                   # ЭДО-обмен + подписи
│  ├─ crm-ai/                   # клиенты, контрагенты, лиды
│  ├─ hr-ai/                    # кадры, отпуска, 360-оценки
│  ├─ docs-ai/                  # СОД + поиск + версии
│  └─ train-ai/                 # дообучение моделей (Unsloth-UI)
├─ shared/
│  ├─ event-bus/                # Redis Streams
│  ├─ models/                   # GGUF-файлы (sym-link)
│  ├─ templates/                # docx/dotx/xlsx-шаблоны
│  └─ policies/                 # RBAC-правила (OPA/Rego)
├─ infra/
│  ├─ docker-compose.yml
│  ├─ nginx.conf
│  └─ init-scripts/
└─ tools/
   ├─ unsloth-trainer/           # CLI-обёртка над Unsloth
   └─ migrator/                 # импорт из BLDR 0.6
```

---

## 5. Пользовательский опыт (UX-скелет)

**Левая панель** = «Процессы» (иконки-плитки):

1. **Тендеры**  
   ├─ Загрузить тендер → LLM извлекает требования → сравнение с РД → риски  
   └─ Сформировать КП → отправить в ЭДО

2. **ТЭО и Сметы**  
   ├─ Авто-смета по РД (ГЭСН)  
   ├─ Сравнение с фактическими ценами поставщиков  
   └─ Экспорт в 1С/Excel

3. **График (ГПР)**  
   ├─ Импорт РД → автокалендарь → критический путь  
   └─ Синхронизация с MS Project / Primavera

4. **Бюджет**  
   ├─ Прямые затраты → НР → СР → прогноз денежного потока  
   └─ Monte-Carlo (LLM-анализ рисков)

5. **Документы (СОД)**  
   ├─ Дерево папок = структура проекта  
   ├─ Версии, diff, blame  
   └─ Поиск (вектор + full-text)

6. **Обучение моделей**  
   ├─ Перетащи PDF → DeepSeek-OCR → Q-A пары → выбор модели → старт  
   └─ Логи, loss-граф, скачать новую GGUF

**Верхняя панель** = глобальный поиск, уведомления, Telegram-бот (qr-код), профиль.

---

## 6. Поток данных (happy path «Тендер → Закрытие»)

```
① Загружаем тендер (PDF)  
  → DeepSeek-OCR → JSON-контракт  
  → LLM выделяет риски, сроки, объёмы  

② Одним кликом «Создать ТЭО»  
  → Авто-смета (ГЭСН-LLM) → сравнение с прайсами поставщиков  
  → Финансовые метрики (NPV, IRR)  

③ Утверждение ТЭО → event «ТЭО_УТВ»  
  → Авто-формирование ГПР (критический путь)  
  → Создание бюджета (прямые + налоги + резервы)  

④ Переход к закупкам  
  → Авто-заявка (ВОМ) → поиск поставщиков → КП → выбор → договор → ЭДО  

⑤ Исполнение  
  → Ежедневные наряды (телеграм-бот) → КС-2/КС-3 → ЭДО → 1С  

⑥ Закрытие  
  → Проверка подписанных КС → ОС-3 → форма-6 → архив в СОД
```

---

## 7. Road-map (MVP → Production)

| Спринт | Цель | Фичи | Готовность |
|--------|------|------|------------|
| **S0** | Инфра | Docker, keycloak, minIO, postgres, qdrant, redis, lm-studio | 100% |
| **S1** | Portal | Auth, меню, профиль, СОД-дерево, загрузка PDF | 80% |
| **S2** | Tender-ai | OCR, LLM-анализ, риски, КП-шаблон, ЭДО-отправка | 60% |
| **S3** | ТЭО-ai | Авто-смета (ГЭСН), сравнение цен, PDF-ТЭО, согласование | 40% |
| **S4** | Budget-ai | Бюджет затрат, ден-поток, Monte-Carlo, Excel-экспорт | 20% |
| **S5** | ГПР-ai | Импорт РД → Gantt, крит.путь, MSP-экспорт, Telegram-уведомления | 10% |
| **S6** | Train-ai | UI-Unsloth, DeepSeek-OCR, Q-A генератор, GGUF-релизы | 0% ← стартуем здесь |

**Каждый спринт = 3 недели.**  
**Релиз MVP** = после S3 (можно показывать заказчику).  
**Production** = после S6 + пентест + бэкап-схема.

---

## 8. Декомпозиция модуля «Train-ai» (Unsloth-UI)

| Экран | Описание | Тех-детали |
|-------|----------|------------|
| **1. Загрузка файлов** | drag-and-drop PDF/DOCX → bucket `uploads/raw` | React-Dropzone, Presigned URL |
| **2. OCR-пайплайн** | кнопка «Извлечь текст» → вызов `deepseek-ocr` (Unsloth container) → JSON-части | FastAPI-background task, WebSocket-прогресс |
| **3. Q-A генератор** | LLM-шаблон «Сгенерируй 10 вопросов и ответов» → ручная правка → approve | Prompt = `ru_qa_system.prompt`, редактор Monaco |
| **4. Выбор модели** | список локальных GGUF (dropdown) + память RAM-индикатор | LM-Studio API `/v1/models` |
| **5. Настройки Unsloth** | эпохи, LR, rank, lora_alpha, max_seq_len → валидация | Pydantic-форма |
| **6. Старт дообучения** | кнопка «Train» → Celery-task → GPU-контейнер Unsloth → артефакты в `models/trained/` | Celery+Redis, nvidia-docker |
| **7. Мониторинг** | live-loss граф, ETA, кнопка «Stop», логи | WebSocket + Chart.js |
| **8. Скачать / Activate** | конвертация LoRA → GGUF (llama.cpp) → кнопка «Сделать активной» | shell-script, symlink в `models/active/` |

**API-контракты** и **React-компоненты** описать в отдельной технической спецификации (можно сгенерировать через Cursor).

---

## 9. Безопасность и compliance

- **PDF- SandBox** — `gvisor` + `pikepdf` (чистка).  
- **Model-RLS** — каждая модель доступна только роли «Инженер» или «Юрист» и т.д.  
- **ЭЦП** — КриптоПро CSP + Диадок-API.  
- **Audit-Log** — каждый вызов LLM + вход/выход записывается в Postgres.  
- **Backup** — ежедневный `pg_dump + restic → S3`.

---

## 10. Что делать прямо сегодня (Next Actions)

1. **Создать репо `bldr-v3` и `docker-compose up` для инфраструктуры (S0).**  
2. **Сгенерировать в Cursor: React-скелет (portal) + FastAPI-шаблон.**  
3. **Написать техническую спецификацию на модуль Train-ai (Unsloth-UI).**  
4. **Подготовить Dockerfile для DeepSeek-OCR (Unsloth-image + GPU).**  
5. **Собрать первый датасет из 100 PDF (ГЭСН-2024) и запустить PoC дообучения.**  

---

**Итог:** мы получили чёткую **высокоуровневую картину**, **модульную архитектуру**, **пошаговый road-map** и **готовый список next-actions**.  
Осталось «размазать» задачи по спринтам и начать кодить в Cursor — уже завтра можно показать живую загрузку PDF и прогресс-бар OCR.